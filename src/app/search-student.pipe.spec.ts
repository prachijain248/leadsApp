import { SearchStudentPipe } from './search-student.pipe';

describe('SearchStudentPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStudentPipe();
    expect(pipe).toBeTruthy();
  });
});
